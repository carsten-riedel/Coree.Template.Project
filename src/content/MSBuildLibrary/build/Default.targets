<Project>

	<Target Name="DemoTarget" AfterTargets="Build">

		<ConcatTask Parameter1="Concat" Parameter2="Me">
            <Output TaskParameter="Result" PropertyName="PropertySet" />
		</ConcatTask>

		<Message Text="Result from $(PropertySet)" Importance="high" />

		<DumpEnvironmentVariablesTask></DumpEnvironmentVariablesTask>
	</Target>

</Project>