<Project>
  <!-- Single source for the framework used by bare app and WiX publishes. -->
  <PropertyGroup>
    <PublishDefaultFramework>__TargetFramework__</PublishDefaultFramework>
  </PropertyGroup>
</Project>
